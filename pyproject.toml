[project]
name = "autoresearch"
version = "0.1.0"
description = "Autonomous classification research (MNIST + MLP)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.0",
    "torch>=2.0.0",
    "torchvision>=0.15.0",
]
